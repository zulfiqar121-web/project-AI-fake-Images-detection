# project-AI-fake-Images-detection
This project is an AI-based fake image detection system. It uses computer vision and a CNN model to classify images as real or fake. First, images are preprocessed, then passed into the model which learns patterns like edges and textures. Finally, the system predicts whether the image is real or AI-generated.
